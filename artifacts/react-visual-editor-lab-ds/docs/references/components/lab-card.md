# LabCard

## Source evidence

- `artifacts/react-visual-editor-torture-test/src/App.tsx`: `TestCard` wraps
  every catalog fixture in an `article.test-card`.
- `artifacts/react-visual-editor-torture-test/src/index.css`: `.test-card`,
  hover lift, primary accent edge, card border, and target-well surface.

## Contract

Bounded source-mapping surface with a thin token border, four-pixel primary
accent edge, restrained hover lift, and enough internal padding for separate
target wells. Do not use it as an unbounded page shell.